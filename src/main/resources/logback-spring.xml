<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="LOGS" value="${LOG_PATH:-./logs}"/>

    <!-- Console: human-readable with level and timestamp; traceId/spanId when present -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %epoch %highlight(%-5level) [%thread] [%X{traceId:-},%X{spanId:-}] %cyan(%logger{40}) - %msg%n%throwable</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- File: rolling daily, keep 3 days; traceId/spanId when present -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOGS}/application.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %epoch %-5level [%thread] [%X{traceId:-},%X{spanId:-}] %logger{40} - %msg%n%throwable</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOGS}/application-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>20MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>3</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- OTLP log export is disabled for test/e2e profiles to keep runs self-contained. -->
    <springProfile name="!test &amp; !e2e">
        <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender"/>
        <root level="INFO">
            <appender-ref ref="Console"/>
            <appender-ref ref="RollingFile"/>
            <appender-ref ref="OTEL"/>
        </root>
    </springProfile>

    <springProfile name="test | e2e">
        <root level="INFO">
            <appender-ref ref="Console"/>
            <appender-ref ref="RollingFile"/>
        </root>
    </springProfile>

    <!-- Application package: INFO by default, DEBUG in dev if needed -->
    <logger name="org.bruneel.thankyouboard" level="INFO"/>
    <!-- Reduce noise from Spring and third parties -->
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.hibernate.SQL" level="INFO"/>
</configuration>
